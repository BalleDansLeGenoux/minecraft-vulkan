#ifndef VULKAN_BUFFER_MANAGER_H
#define VULKAN_BUFFER_MANAGER_H

#include <vulkan/vulkan.h>

class BufferManager {
public:
    // VkBuffer createVertexBuffer();
    // VkBuffer createIndexBuffer();
    // VkBuffer createComputeBuffer();
};

#endif